import { platformBrowserDynamic } from '@angular/platform-browser-dynamic';

import { TodoIonicframeworkAppModule } from './app.module';

platformBrowserDynamic().bootstrapModule(TodoIonicframeworkAppModule);
