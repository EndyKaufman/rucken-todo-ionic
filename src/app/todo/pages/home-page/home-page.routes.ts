import { IRoute } from '../../shared/interfaces/route.interface';
import { HomePageComponent } from './home-page.component';
export const HomePageRoutes: IRoute[] = [
  { data: { title: 'Home', icon: 'home' }, component: HomePageComponent }
];

